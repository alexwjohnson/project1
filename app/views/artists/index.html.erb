<h1>All Artists</h1>

<% @artists.each do |artist| %>
    <%= image_tag artist.image, :class => 'thumb' if artist.image.present? %>
        <ul>
            <li class="no-bullets"><%= link_to artist.name, artist %></li>
        </ul>
<% end %>